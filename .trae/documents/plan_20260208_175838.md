## Implementation Plan: Fix Dashboard UI Issues

### Step 1: Fix InventoryDonut Component

* **File Location:** `frontend/components/visualizations/InventoryDonut.tsx`

* **Fix 1: Data Safety Check**
  * Add check for empty data array
  * Return "No Data" placeholder when data is empty

* **Fix 2: Maintain Structure**
  * Keep the existing w-48 h-48 container
  * Ensure component doesn't crash when data is missing

### Step 2: Fix StrategicRadar Component

* **File Location:** `frontend/components/visualizations/StrategicRadar.tsx`

* **Fix 1: Data Safety Check**
  * Add check for empty data array
  * Return "No Data" placeholder when data is empty

* **Fix 2: Maintain Height**
  * Keep the existing h-64 container
  * Ensure component doesn't crash when data is missing

### Step 3: Verify ReportView.tsx Layout

* **File Location:** `frontend/components/ReportView.tsx`

* **Verify 1: Data Props**
  * Confirm data is being passed correctly with fallbacks
  * `vizData.radar_data || []`
  * `vizData.inventory_mix || []`

* **Verify 2: Layout Structure**
  * Confirm KeyInsightCards is at top
  * Confirm Charts Grid is in middle
  * Confirm LogicFlow is at bottom
  * Confirm full_markdown_report is in collapsed details tag

### Step 4: Add Loading States

* **Enhancement:** Add simple loading states for components
* **Purpose:** Prevent flash of content during data loading

### Expected Outcome

* **✅ No More Flash/Disappear:** Components will handle empty data gracefully
* **✅ Clean Layout:** Text-heavy report will be collapsed by default
* **✅ Data Safety:** All components will have proper fallback values
* **✅ Responsive Design:** Charts will maintain consistent height