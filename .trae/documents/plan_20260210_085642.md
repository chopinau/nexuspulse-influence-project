I have analyzed the current prompt architecture and identified the need for a more structured "Prompt Assembly Line" to ensure consistent and predictable outputs.

### **Analysis & Refactoring Strategy**

The current system has disjointed prompt logic across `prompts.py` (Strategy) and `agent_skills.py` (Role). I will unify this into a single, cohesive assembly function in `prompts.py` that builds the final system prompt in three explicit layers, as requested.

#### **1. Refactor `python_backend/prompts.py`**
I will replace the simple `get_strategy_prompt` with a comprehensive `assemble_system_prompt` function.

*   **Layer 1: The Skillset (Role)**
    *   Migrate the specific role definitions from `agent_skills.py` (or define them explicitly if missing) into this function.
    *   **Logic:**
        *   `TIKTOK_MARKETING`: "Viral Content Director... output must include 3-second visual hooks..."
        *   `AMAZON_SEO`: "A9 Algorithm Expert... focus on keyword density..."
        *   `CROSS_BORDER_CFO`: "Ruthless CFO... focus on Unit Economics..."
        *   `TIKTOK_RISK`: "Compliance Lawyer... focus on Ban Probability..."
        *   `GENERAL`: "McKinsey Consultant... focus on SWOT..."

*   **Layer 2: The Personality (Strategy Mode)**
    *   Retain and refine the existing logic:
        *   `incubation`: "Conservative Guardian... VETO risky ideas..."
        *   `growth`: "Ruthless Growth Hacker... High ROI... Ignore minor warnings..."
    *   **Crucial Update:** Explicitly add the "Fatal Risk Veto" instruction to the `growth` mode to prevent illegal advice even in aggressive settings.

*   **Layer 3: The Focus (Pain Point)**
    *   Add dynamic pain point handling:
        *   **Provided:** "Focus STRICTLY on solving: '{pain_point}'."
        *   **Missing:** "Identify the MOST URGENT pain point automatically..."

#### **2. Update `python_backend/report_engine_only.py`**
*   **Integration:** Replace the current fragmented prompt loading with a single call to `assemble_system_prompt(role, strategy, pain_point)`.
*   **Logging:** Add a specific log line: `logger.info(f"🧠 Logic Selected: {role} + {strategy} + {pain_point_status}")`.
*   **Debate Engine:** Ensure the assembled prompt is correctly passed to the **Moderator**, **Bull**, and **Bear** agents. The Moderator needs the full context to judge effectively.

#### **3. Verification Plan**
*   **Simulation:** Run the engine with different combinations (e.g., `TIKTOK_MARKETING` + `growth` vs. `TIKTOK_RISK` + `incubation`) and verify the `Logic Selected` log and the resulting "System Instruction" output.

### **Execution Steps**
1.  **Modify `python_backend/prompts.py`**: Implement `assemble_system_prompt`.
2.  **Modify `python_backend/report_engine_only.py`**: Integrate the new function and add logging.
3.  **Verify**: Run a test command to confirm the prompt assembly.
