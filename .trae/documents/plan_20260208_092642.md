# Implementation Plan: Enforce Live Data & Remove Mocks

## 1. Scan & Delete Mocks
- **Search**: Grep for `MOCK_NEWS`, `MOCK_TRENDS`, and hardcoded lists in `python_backend/news_engine.py` and `python_backend/report_engine_only.py`.
- **Action**: Remove or comment out these static structures.

## 2. Refactor `news_engine.py`
- **Tavily Integration**: Verify `fetch_market_news` prioritizes `TavilyClient.search`.
- **LLM Fallback**: Enhance the fallback logic. If Tavily fails/returns empty, call DeepSeek to generate *dynamic* trends based on the `topic` (not static text).
- **Format**: Ensure strict return type `List[Dict]` with `title`, `source`, `url` (mock URL if synthesized).

## 3. Update `report_engine_only.py`
- **Integration**: Ensure `collect_intelligence` or the final JSON output includes data fetched from `news_engine.fetch_market_news`.
- **Clean up**: Remove any fallback static text in the report generation phase if it exists.

## 4. Verification
- **Test**: Run a test script to verify `fetch_market_news("Bitcoin")` returns live data (or dynamic LLM data) and not the old static list.