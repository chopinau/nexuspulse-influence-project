## Implement `generate_ceo_report` Function

### Overview
This implementation will add a CEO-focused report generation function that creates minimalist Markdown reports based on the structured decision data returned by the `analyze_with_llm` function.

### Implementation Steps

1. **Add `generate_ceo_report` Function**
   - Add the function to `python_backend/report_engine_only.py`
   - Update the data extraction to work with the nested structure returned by `analyze_with_llm`
   - Ensure it correctly extracts CEO verdict, risk scores, and key metrics from the nested data structure

2. **Update Data Extraction Logic**
   - Modify the function to extract data from the correct nested keys:
     - CEO verdict from `decision_data["ceo"]["verdict"]`
     - Risk score from `decision_data["compliance"]["risk_score"]`
     - DOS from `decision_data["supply"]["dos"]`
     - ROI from `decision_data["marketing"]["roi"]`
   - Ensure all data extraction uses safe `get` methods to prevent errors

3. **Test the Implementation**
   - Verify the function can process data from `analyze_with_llm`
   - Ensure it generates properly formatted Markdown reports
   - Test with different decision scenarios to validate report generation

### Expected Outcome
A CEO-focused report generation function that creates minimalist Markdown reports following the 30-second reading principle, correctly extracting data from the nested structure returned by the `analyze_with_llm` function.