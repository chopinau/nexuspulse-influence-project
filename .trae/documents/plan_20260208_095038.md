I will implement the RAG Knowledge Base system as requested.

### **Plan of Action:**

1.  **Create `python_backend/knowledge_base.py`:**
    *   Implement `SimpleRAG` class with:
        *   `__init__`: Initialize empty storage.
        *   `ingest(text, doc_id)`: Chunk text (500 chars) and store in memory.
        *   `query(topic)`: Perform keyword-based retrieval.

2.  **Integrate into `report_engine_only.py`:**
    *   **Import:** `from knowledge_base import SimpleRAG`
    *   **Initialize & Ingest:** inside `analyze_with_llm` (or global scope), initialize `SimpleRAG` and ingest the hardcoded "Amazon 2026 Compliance Policy".
    *   **Query & Inject:**
        *   Call `rag.query(topic)` to get relevant chunks.
        *   Inject these chunks into the `skill_context` or a new `internal_knowledge` section within the LLM prompts (`BULL_PROMPT`, `BEAR_PROMPT`, `MODERATOR_PROMPT`), formatted under `### INTERNAL KNOWLEDGE`.

### **Verification:**
*   Run the engine with a query like "Amazon compliance" to verify that the RAG chunks are retrieved and included in the context (I will check the logs/output).
