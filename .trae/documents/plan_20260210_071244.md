I have analyzed the codebase and identified the gaps between the current implementation and your Dual-Mode Blueprint.

### **Gap Analysis Findings**

1.  **Layer 1: Frontend & API (Mode Switch)**
    *   **Status:** Partially Complete. I previously added the selector to `PersonaInput.tsx` and updated the API route to pass `strategy_mode`.
    *   **Gap:** The Python backend (`report_engine_only.py`) does **not** yet accept or process the `strategy_mode` argument.

2.  **Layer 2: Dynamic Prompt Injection**
    *   **Status:** Missing.
    *   **Gap:** `python_backend/prompts/prompts.py` is for the legacy engine. The active engine uses `agent_skills.py` and inline prompts.
    *   **Plan:** I will create a new `python_backend/prompts.py` to centralize the "Strategy Mode" prompts (Guardian vs. Sniper) and "Veto Power" logic, keeping it separate from the existing Persona prompts.

3.  **Layer 3: Knowledge Base (RAG)**
    *   **Status:** Incomplete.
    *   **Gap:** The directory structure (`brand_guide`, `finance_models`, etc.) is missing. The `SimpleRAG` loader currently only scans the root `data/knowledge` folder and ignores subdirectories.
    *   **Plan:** I will create the directories and update `report_engine_only.py` to explicitly load the relevant subfolders based on the selected mode.

4.  **Layer 4: Agent Workflow (The Debate)**
    *   **Status:** Missing.
    *   **Gap:** The "Veto Power" logic (Compliance vs. CFO) is not implemented in the debate instructions.

---

### **Implementation Plan**

#### **Step 1: Infrastructure & Data**
*   **Create Directories:**
    *   `data/knowledge/trends`
    *   `data/knowledge/brand_guide` (for Incubation Mode)
    *   `data/knowledge/finance_models` (for Growth Mode)

#### **Step 2: Backend Logic (Prompts)**
*   **Create `python_backend/prompts.py`:**
    *   Define `get_strategy_prompt(mode)` to return:
        *   **Incubation Mode:** "Guardian/Teacher" tone, Compliance Agent has VETO power.
        *   **Growth Mode:** "Sniper/CFO" tone, CFO Agent has VETO power.

#### **Step 3: Backend Execution (`report_engine_only.py`)**
*   **Update Argument Parsing:** Add support for `--strategy_mode`.
*   **Update RAG Loading:**
    *   Modify `SimpleRAG` usage to load `compliance_rules` + `trends` (Base).
    *   Conditionally load `brand_guide` OR `finance_models` based on mode.
*   **Inject Prompts:**
    *   Wrap the existing `BULL_PROMPT`, `BEAR_PROMPT`, and `MODERATOR_PROMPT` with the new Strategy instructions.

#### **Step 4: Verification**
*   **Frontend Check:** Confirm the "Execute" button sends the correct signal.
*   **Simulation:** Run the Python script manually with `--strategy_mode` to verify prompt injection and RAG loading.
