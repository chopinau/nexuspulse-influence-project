# Implementation Plan: Multi-Expert Architecture

## 1. Create Skills Library (`python_backend/agent_skills.py`)
- Create a new file `python_backend/agent_skills.py`.
- Define `SKILL_SET` and `SEARCH_PROMPTS` dictionaries as requested (fixing the `SKILL_ SET` typo).

## 2. Implement Backend Router (`python_backend/report_engine_only.py`)
- **Import Skills**: Import `SKILL_SET` and `SEARCH_PROMPTS`.
- **Argument Parsing**: Update `main()` to accept a `--mode` argument (default: "GENERAL").
- **Search Logic**: Update `collect_intelligence` to accept `mode`.
  - Append the `search_suffix` from `SEARCH_PROMPTS[mode]` to the query.
- **Analysis Logic**: Update `analyze_with_llm` to accept `mode`.
  - Retrieve the persona definition from `SKILL_SET[mode]`.
  - **Prompt Injection**: Instead of replacing the entire robust `MODERATOR_PROMPT`, I will **inject** the selected persona (Role, Focus, Output definition) into the beginning of the `MODERATOR_PROMPT`.
  - This ensures we keep the Markdown/Mermaid/JSON formatting rules while adopting the specific persona.
  - Specifically, I will replace the "You are **The Moderator**..." section with the content from `SKILL_SET[mode]`.

## 3. Update API Route (`app/api/agent-forum/route.ts`)
- Parse `mode` from the request body.
- Pass the `--mode` flag and value to the Python subprocess.

## 4. Update Frontend UI (`app/page.tsx`)
- **State**: Add `const [mode, setMode] = useState("GENERAL");`.
- **UI**: Add the `<select>` dropdown component in the search bar area.
- **Logic**: Update `handleGenerate` to send `mode` in the JSON body.

This plan executes all 3 requested steps in a single turn, ensuring the system transforms into a multi-expert platform while maintaining system stability.